{
  "name": "AsynchronousProgramming",
  "version": "1.0.0",
  "description": "Design a component (a `function` or a `class`) that accepts an array of URLs and allows to fetch multiple resources in parallel, returning their `.text` representation. Provide a parameter so that the caller can limit the number of concurrent fetches run in parallel.",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist/ && tsc --project .",
    "start": "npm run build && node dist/index.js",
    "test": "jest"
  },
  "author": "Kenneth Jimmy",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/node16": "^1.0.3",
    "@types/jest": "^28.1.6",
    "@types/node": "^17.0.42",
    "@typescript-eslint/eslint-plugin": "^5.27.1",
    "@typescript-eslint/parser": "^5.27.1",
    "eslint": "^7.32.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.24.2",
    "jest": "^28.1.3",
    "prettier": "^2.6.2",
    "ts-jest": "^28.0.7",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.3"
  },
  "dependencies": {
    "axios": "^0.27.2"
  }
}
